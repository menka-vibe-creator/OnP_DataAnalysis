"""Custom tools for the data analysis agent.

Add tool modules here and import them in agent.py as needed.
Each tool should be defined as an async function and registered
via an MCP server (see claude_agent_sdk.create_sdk_mcp_server).
"""
